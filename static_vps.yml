---
- name: Setup user
  hosts: all
  remote_user: root
  vars:
    password: sf.fmOdgCOEVI

  roles:
    - { role: common }

- name: Setup user environment
  hosts: all
  remote_user: deploy

  roles:
    - { role: manala.git }
    - { role: manala.zsh }
    - { role: mashimom.oh-my-zsh, ohmyzsh_theme: 'aussiegeek' }
